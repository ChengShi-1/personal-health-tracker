import type {ReactNode} from 'react'; export function ChartCard({title,subtitle,children,empty=false}:{title:string;subtitle?:string;children:ReactNode;empty?:boolean}){return <section className="panel chart-card"><div><h3>{title}</h3>{subtitle&&<p className="muted">{subtitle}</p>}</div>{empty?<div className="empty">暂无足够数据</div>:<div className="chart-wrap">{children}</div>}</section>}
